{
  "name": "goody-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "serve": "node serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
